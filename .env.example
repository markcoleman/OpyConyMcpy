# OpCon API Configuration

# OpCon API base URL (required)
OPCON_BASE_URL=https://opcon-server.example.com:9010

# Authentication - provide either TOKEN or USERNAME + PASSWORD

# Option 1: Token authentication (recommended)
OPCON_TOKEN=your-token-here

# Option 2: Username/password authentication
# OPCON_USERNAME=your-username
# OPCON_PASSWORD=your-password

# TLS/SSL verification (optional, default: true)
# Set to false only in development with self-signed certificates
OPCON_VERIFY_TLS=true

# Metrics and Monitoring (optional)

# Enable Prometheus metrics collection (default: false)
OPCON_METRICS_ENABLED=true

# Port for metrics HTTP server (default: 9090)
OPCON_METRICS_PORT=9090
